docker run hello-world
docker compose version
docker ps
docker ps -a // list all containers
docker rm magical_volhard
docker rm 8282c285ecea ( id do container) // delete
docker container prune // delete all exited containers

echo. > docker-compose.yml criar o arquivo pelo cmd
notepad docker-compose.yml

docker-compose down -v
docker-compose -f docker-compose.dev.yml up --build frontend

docker-compose -f docker-compose.dev.yml down -v && docker-compose -f docker-compose.dev.yml up --build

docker-compose -f docker-compose.dev.yml up --build
docker-compose -f docker-compose.dev.yml up --build -d ( roda em segundo plano, deixa o terminal livre)

http://backend:8092/api/auth/users

docker-compose up --build
docker-compose up -d


cd Backend/photo-map
mvn clean package 

docker logs postgres-container

docker exec -it postgres-container psql -U postgres -d photo_map

docker-compose down && docker-compose up --build














